# Copy to config/communication/calling.secrets.yaml on a real device.
# This file is intentionally untracked and loaded after the non-secret authored config.

secrets:
  sip_password: ""
  sip_password_ha1: ""
