# www — the yoyopod.com landing page

The public teaser page served at `https://yoyopod.com`. A standalone static
Astro site: deliberately a single coming-soon screen ("Something small is
coming.") with a parked ordering-availability signup and a link to the docs
site. The full product story stays unpublished until ordering opens.

`NOTIFY_ENABLED` is deliberately `false`: no email is collected until the
contact mailbox, hosting AVV, and retention procedure are ready. When enabled,
the form posts to `NOTIFY_ENDPOINT` (`src/consts.ts`, default `/api/notify`),
served by the tiny self-hosted collector in `server/notify-collector.mjs`. See
`docs/operations/WEB_DEPLOY.md` for the legal and infrastructure gates.

**Parked sections:** the below-fold components in `src/components/`
(ProblemSection, SolutionSection, ValueProps, ColorwayShowcase, HowItWorks,
WhyNotAPhone, BuiltForBothSides, ClosingCta, Header, Footer on the index) are
built and copy-complete from `docs/product/LANDING_PAGE_POSITIONING.md`, but
intentionally not mounted on the page. Re-add them in
`src/pages/index.astro` when the product is ready to tell its full story.

The design follows the locked coming-soon direction (the "yoyopod web pack"
mockup: navy `#2b2836` / cream `#f0eee9` / cyan `#05cae9`, four pastel finish
chips, the animated line-art device teaser) and the brand logo pack rules in
`docs/brand/README.md` (lowercase bounce-p wordmark, Figtree 900). Below-fold
copy is sourced from `docs/product/LANDING_PAGE_POSITIONING.md` (the decided
homepage spec).

## Commands

Run from this directory:

| Command           | Action                                            |
| ----------------- | ------------------------------------------------- |
| `npm install`     | Install dependencies                              |
| `npm run dev`     | Dev server at `http://localhost:4323`             |
| `npm run build`   | Build the production site to `./dist/`            |
| `npm run preview` | Preview the built site locally                    |
| `npm run og`      | Regenerate `public/og.png` (the social share image) |

## Deployment

Built and uploaded manually to the VPS — see `docs/operations/WEB_DEPLOY.md`
for the full runbook (build script, upload commands, nginx, TLS).

## Asset provenance

All assets are committed copies; regenerate by re-copying from the source of
truth:

| File | Copied from |
| --- | --- |
| `src/assets/device/render-*.png` | `docs/hardware/enclosure/v2/colorways/` |
| `src/assets/device/yoyopod-v2-ortho-clean.svg` | `docs/hardware/enclosure/v2/` |
| `src/assets/brand/yoyopod-glyph.svg` | `pitch/src/assets/brand/` |
| `src/assets/brand/yoyopod-cream-1024.png` | `docs/brand/logo/png/` |
| `public/favicon.svg` | `docsite/website/public/favicon.svg` |
| `public/media/yoyopod-tour.mp4` | `docs/product/PRODUCT_VIDEO.mp4` (24 s, silent) |
| `public/og.png` | generated by `scripts/generate-og.mjs` |

Every device image on the page carries the caption required by
`docs/product/README.md`: the renders show the **V2 design study, not shipped
hardware**.

## Before public launch

- `/imprint` (§ 5 DDG) and `/privacy` (DSGVO) are filled in. Remaining
  chores: create the `privacy@yoyopod.com` mailbox, accept Hetzner's data
  processing agreement (AVV), and keep nginx logrotate at 14 days (see
  `docs/operations/WEB_DEPLOY.md`).
- Both legal pages are bilingual: German first, English below.
- Keep `NOTIFY_ENABLED` false and the nginx endpoint closed until the mailbox
  works, the AVV is accepted, and the retention procedure is verified.
- `src/consts.ts` holds the configurable URLs (`DOCS_URL`, `GITHUB_URL`).

## Ideas parked for v2

- Interactive 3D model via `<model-viewer>` — the GLBs exist at
  `docs/hardware/enclosure/v2/model/*.glb` (~1 MB each); skipped in v1 to keep
  the page light. Use a lazy single GLB with the PNG render as poster.
- Re-render the colorway PNGs at 2× (source renders are 342×464 and go soft
  above ~300 CSS px on high-DPI screens).
